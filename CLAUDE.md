# Claude Code Plugin

## Mobile Mode

When a message starts with `[M]`, it means the user is on a mobile device (phone).
In mobile mode:
- Keep responses short and concise (2-3 sentences max)
- No tables, no box-drawing characters, no decorative separators
- No code blocks unless specifically requested
- Use plain text, bullet points only when necessary
- Skip explanations unless asked
